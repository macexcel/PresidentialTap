<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="macexcel.example.com.mypresidentialtap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Main"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Candidates"
            android:label="@string/title_activity_candidates"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>

        <activity android:name=".Help" >
        </activity>

        <activity
            android:name=".Binay"
            android:label="@string/title_activity_binay"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>

        <activity android:name=".Duterte" >
        </activity>

        <activity android:name=".Miriam" >
        </activity>

        <activity android:name=".Poe" >
        </activity>

        <activity android:name=".Roxas" >
        </activity>

        <activity
            android:name=".stats"
            android:label="@string/title_activity_stats"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
